---
interface Props {
	skills: string[];
}

const { skills } = Astro.props;
---
<div class="flex flex-col">
    <span class="text-2xl font-bold text-stone-800 uppercase">Skills</span>
    <span class="text-sm">{skills.join(", ")}</span>
</div>